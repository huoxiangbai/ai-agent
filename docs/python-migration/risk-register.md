# Migration risk register

Last updated: 2026-09-25

| ID | Risk | Evidence/impact | Mitigation and exit condition | Phase |
|---|---|---|---|---|
| R-01 | Java tests were configured to skip | parent POM hard-coded `<skipTests>true</skipTests>` | **fixed in the working tree but UNCOMMITTED** (`M pom.xml`, `M Reactor-agent-app/pom.xml`, `M Reactor-agent-domain/pom.xml`). Default is now false and the legacy app suite is visible. Exit condition: the fix is committed and no longer depends on a dirty tree | 0/1 |
| R-02 | Java application test baseline is red | real run: app module executed 469 tests with 12 failures and 53 errors; most involve missing ignored configs, path assumptions and stale fixtures | keep stable modules as the blocking CI gate; run the full legacy baseline visibly and retire categories deliberately | 0/1 |
| R-03 | SSE is richer than a basic text stream | frontend merges by event IDs, orders and nested tool IDs; disconnect is not terminal | golden SSE recorder plus lifecycle/cancellation tests before route cutover | 2/5 |
| R-04 | Active run state is partly in memory | stop/inject/follow and pending registries require backend affinity | stable visitor-cookie cohort; all run-control routes use the same upstream; restart tests | 5/9 |
| R-05 | Cross-runtime shared writes can corrupt state | 33 tables include CAS workflows and start/finish ledgers | table/operation ownership matrix, exact-path cutover, no dual write | all |
| R-06 | MyBatis semantics may differ from ORM defaults | generated keys, conditional updates, null handling and dynamic filters | async SQLAlchemy Core with SQL-equivalence tests; defer ORM refactors | 3/4 |
| R-07 | Timezone/serialization drift | JDBC explicitly uses `Asia/Shanghai`; Java date serialization may differ | boundary fixtures for naive/aware times, nulls and JSON output | 2-4 |
| R-08 | External tests may spend money or leak credentials | model/image/search/MCP calls are runtime-configured | local fakes and sanitized recordings in automation; explicit manual real-provider suite | all |
| R-09 | Generic retries can duplicate side effects | tools create files, ledger rows and remote jobs | retry only idempotent operations; add idempotency keys where already supported | 5-8 |
| R-10 | Workspace escape or command injection | Skill and shell tools execute in per-session workspaces | realpath containment, symlink and traversal tests, bounded subprocess API | 6 |
| R-11 | Java executor behavior is not equivalent to unbounded asyncio | production LLM concurrency reaches 128 and rejects excess work | explicit semaphores/queues, busy response, TaskGroup cancellation and metrics | 5-8 |
| R-12 | Memory/replay can duplicate or lose semantic events | compaction protects messages; replay rebuilds UI tool outputs | recorded-session parity, tool call/result pairing and replay idempotency tests | 7/8 |
| R-13 | Data Agent has additional datastores and SQL dialects | ES7, Qdrant, ClickHouse/JDBC and Calcite utilities | migrate last, with service-specific fixtures and query safety tests | 8 |
| R-14 | No repository CI workflow was found | regressions relied on local execution | **added in the working tree but UNCOMMITTED** (`.github/workflows/python-backend.yml`, `.github/workflows/java-baseline.yml` are untracked). Exit condition: the workflows are committed and green on a clean checkout | 1 |
| R-15 | Existing user changes could be overwritten | `reactor-tool/uv.lock` is modified and `INTERVIEW_STUDY_GUIDE.md` is untracked | never edit/revert these files; review `git status` at every stage gate | all |
| R-16 | Nginx percentage routing can split related run-control calls | a run and its follow/stop/inject must share in-memory owner | hash the existing visitor cookie and route the whole Agent control family together | 9 |
| R-17 | **New regressions in `Reactor-agent-app` never block CI** | `java-baseline.yml` gates only `mvn -pl Reactor-agent-case -am` (api/domain/infrastructure/types/trigger, not app); the app suite runs in `legacy-application-baseline` with `continue-on-error: true`. The 65 known failures are visible, but so is any new regression | accepted for now (decision 2026-09-22): documented as a known blind spot. Exit condition: either a baseline-snapshot gate that blocks only on new failures, or the app suite becomes green | 0/1 |
| R-18 | **The Java app baseline is structurally irreproducible** | `.gitignore` excludes `/Reactor-agent-app/src/main/resources/application-test.yml` while `application.yml` and `application-prod.yml` are tracked. A fresh clone can never make the legacy suite green, and the 65 failures cannot be independently reproduced | tracked as an open risk. Do not widen the ignore. Exit condition: a sanitized `application-test.yml` template is committed, or the failure list is captured as a reproducible baseline fixture | 0/1 |
| R-19 | **Production Java source is excluded from version control** | `.gitignore` excludes `…/domain/agent/service/execute/auto/step/` and `…/auto1/step/` ("自执行旧目录仅保留本地"). Clean checkouts silently lose that code and cannot reproduce local builds | tracked as an open risk. Exit condition: the directories are either committed or their removal is an explicit approved decision | 0/1 |
| R-20 | Frontend calls three routes with no implementation | `ui/src/services/agent.ts` calls `/web/api/login`, `/web/api/getWhiteList`, `/web/api/reactor/apply`; no controller/mapper/config implements them | pending determination (dead UI code vs undocumented external surface). UI is out of migration scope. Listed in the deferred registry | open |
| R-21 | Legacy `/1/web/...` path can issue cookies on a 404 | `VisitorIdentityFilter` matches `/1/web/api/v1/gpt/queryAgentStreamIncr` but no controller maps `/1/` | recorded, not deleted. Exit condition: an explicit decision to keep or drop the alias, then a filter/controller change in one approved task | open |
| R-22 | Single-writer rule has no technical enforcement | both backends share the read-write MySQL account `reactor` | **phase-3 half closed 2026-09-23.** The phase-3 read-only account `reactor_py_ro` is provisioned (`db/migrations/20260923_provision_phase3_readonly_account.sql`: `USAGE` + `SELECT` only; verified that INSERT/UPDATE/DELETE/TRUNCATE/DROP/ALTER/CREATE/INDEX all raise `ERROR 1142` and that the project's own `asyncmy` driver reads through it). `reactor-backend-python` now connects as `reactor_py_ro` (`docker-compose.yml` → `REACTOR_PY_MYSQL_USER`/`REACTOR_PY_MYSQL_PASSWORD`, deliberately *not* `MYSQL_USER`/`MYSQL_PASSWORD`) and `tests/integration/test_readonly_account_denies_writes.py` asserts the 1142s. **Phase-4 write half closed 2026-09-25**: `reactor_py_featured_writer` (`db/migrations/20260923_provision_phase4_featured_writer_account.sql`) holds `SELECT` on everything plus `INSERT, UPDATE` on `ai_agent_featured_conversation` **only** — no `DELETE`, no DDL, `ERROR 1142` elsewhere, asserted by `tests/integration/test_featured_writer_account_scope.py`. Together with the `REACTOR_PY_FEATURED_ADMIN_WRITE_OWNER` fail-closed flag this is the two-layer fence in `database-ownership.md`. Residual: the account's password lives only in the test env / operator channel (R-34), and *other* write domains still share `reactor`. Exit condition per domain: provision that domain's writer account before switching its routes | 3-4 |
| R-23 | Docs claimed a schema tool that does not exist | `AGENTS.md` and `database-ownership.md` required Alembic; no Alembic config/dependency exists. Actual practice is `db/migrations/*.sql` | corrected in both documents on 2026-09-22. Exit condition: a schema change either introduces Alembic in its own approved task or explicitly approves reviewed plain-SQL migrations | all |
| R-24 | Frontend consumer tests have no CI enforcement | `ui/package.json` has `vitest run` (~85 test files) but neither workflow runs it, while `PLANS.md` lists consumer tests as acceptance evidence | document the gap; add a UI job only in an approved CI task | all |
| R-25 | **The documented Java "stable baseline" never compiles the HTTP layer** | `mvn -B -pl Reactor-agent-case -am` builds only parent/api/types/domain/case. `Reactor-agent-trigger` (all 108 routes) and `Reactor-agent-infrastructure` (all mappers) are not dependencies of `case`, so a compile or type error there is invisible to the blocking gate. Verified 2026-09-22 | widen the blocking gate to also build `Reactor-agent-trigger` and `Reactor-agent-infrastructure` (compile-only is enough) in an approved CI task | 0/1 |
| R-26 | **A machine-level HTTP proxy can silently kill local recording** | `urllib.request.getproxies()` returns `http://127.0.0.1:1082` on this machine with `NO_PROXY` unset and **no proxy environment variables set**, so `env \| grep -i proxy` shows nothing. httpx with `trust_env=True` (its default) routes even `127.0.0.1` through it and recording fails with a bare `RemoteProtocolError: Server disconnected without sending a response`. Hit 2026-09-23 | every contract-tool `httpx.AsyncClient` is constructed with `trust_env=False` (that flag is load-bearing, do not drop it). Exit condition: a CI job that runs the recording path on a clean runner proves the tool works without host proxy configuration | 2+ |
| R-27 | **Cookie attributes depend on the active Spring profile, so goldens are profile-scoped** | `application.yml` defaults `autobots.execution.visitor-cookie.secure: false`, but `application-prod.yml` overrides it to `true` under `spring.config.activate.on-profile: prod`. The recorded golden therefore carries `Secure`. Contrary to the plan's assumption, the cookie is **not** attribute-stable across profiles | record under a named, documented profile (`prod` today) and treat a golden recorded under any other profile as incomparable. Exit condition: the profile is pinned in the recording command and restated in every golden-producing recipe | 2/3 |
| R-28 | **Replay-time generated values only surface at record time** | cases 4/6 emit `resultMap/eventData/taskId`, a random UUID minted by the replay projector and **not stored in the DB**. Two consecutive recordings differed (`2d546ca0-…` vs `e1c6af8f-…`). The plan's guess (`useTimes`) never appears in the payload at all | declared per-case in `initial.json`. Any new nondeterministic leaf must be declared the same way — never promoted to a global ignore. Exit condition: phase-3 route work re-runs the repeatability check (record twice, `cmp`) after every manifest change | 2/3 |
| R-29 | **Filesystem inputs drift and expire goldens** | `skills[]` is produced by scanning `${user.dir}/runtime/skills`, which evolves with the repo. A golden recorded against it goes stale through no code change | recording pins `--autobots.autoagent.skill.directories[0]=backend-python/tests/contract/fixtures/skills`. Contract baselines must pin *all* inputs, not just the database. Exit condition: any new filesystem-derived response field gets a pinned fixture equivalent before its case is recorded | 2/3 |
| R-30 | **The allowlist carries two semantics on one pointer list** | capture-time `normalize_json` replaces a leaf value; compare-time `drop_additive` deletes a candidate-only leaf. A pointer absent at its *final* object key is a deliberate no-op so additive declarations work; a missing *intermediate* key used to be a silent no-op too, which meant a mistyped allowlist showed a false pass | structural errors now raise `ValueError` (non-`/` pointer, non-integer or out-of-range list index, descending into a scalar, `*` over a scalar), and the split rule is documented in `models.py` and `api-contracts.md`. Exit condition: keep the eight `test_contract_normalize.py` cases green whenever the walker changes | 2/3 |
| R-31 | **Java and `mysqld` boot on this host is fragile in ways the docs did not say** | zsh treats `--autobots.autoagent.skill.directories[0]=...` as a glob (`no matches found`) unless the whole argument is quoted; MySQL 9.3 rejects the removed `mysql_native_password=ON` variable with `[ERROR] [MY-000067]`; the Homebrew `my.cnf` and the compiled-in default datadir both point at a path that must not be touched, so `mysqld` needs `--no-defaults` **and** an explicit `--datadir` every time. Two more added 2026-09-23: surefire 2.6 with `-Dtest='FeaturedConversation*'` also runs the **nested stub classes** as tests, each reporting `initializationError` (7 of 28 runs), and `-Dtest=…` aborts in test-less upstream modules (`Reactor-agent-api` etc.) with "No tests matching pattern" unless `-Dsurefire.failIfNoSpecifiedTests=false` is passed | all three original quirks are recorded in `tests/contract/README.md` and the ExecPlan's Concrete Steps; the two surefire quirks are recorded in the ExecPlan's Surprises & Discoveries and Concrete Steps. Per R-02/R-18 read the **class-level** result (`FeaturedConversationPublicQueryApplicationServiceTest` 13/13), not the module total. Two more found 2026-09-25 while drilling the admin slice: **macOS BSD `xargs` does not honour GNU `-I{}`**, so `command -v nginx \| xargs -I{} {}` dies with `xargs: {}: No such file or directory` before nginx ever runs (`tests/cutover/admin_drill.sh` now uses `"$(nginx_bin)" -p … "$@"` like `drill.sh`); and a `tests/`-relative import run as a script fails with `ModuleNotFoundError: No module named 'tests'` because `tests/` is a PEP 420 namespace package with no `__init__.py` — invoke with `PYTHONPATH=$BACKEND_PY`. Exit condition: a single scripted bring-up that a clean machine can run without rediscovering these | 2/4 |
| R-32 | **`reactor-contract` silently drops a case at *compare* time and still exits 0** | the comparator re-substitutes `${CONTRACT_*}` env vars during comparison; a case whose variable is unset lands in the report's `skipped` array (`"featured-detail-fixture: missing CONTRACT_FEATURED_ID"`) rather than in `cases`, and the process still exits 0. Observed 2026-09-23: a 3-case manifest produced `cases: 2, skipped: 1` and looked green. The diff field is named `differences` (empty → `[]`), not `diffs` | the Concrete Steps comparison command now exports the same `CONTRACT_*` set as the recording command. **Gate on `len(cases)` and `skipped == []`, never on exit code alone.** Observed green 2026-09-25: `phase4-featured-admin.json` produced `cases=15 skipped=[] differing=[]`. Exit condition still open: the CLI fails non-zero when `skipped` is non-empty in comparison mode, or the report schema puts skipped cases where an acceptance check cannot miss them | 2/3 |
| R-33 | **Contract recording and edge-case integration must never share a seeded database** | `featured_read_edge_seed.sql` inserts ONLINE rows with `sort_order` 500/400/200/150, which push the contract fixture rows off the first page of home/list; home and list immediately went from 2 rows to 6 rows and produced 16–17 differences. This is fixture contamination, not an implementation defect | the two suites own their seeds exclusively: contract recording / R-28 re-check run against a DB seeded with **only** `contract-seed.sql`; the integration suite owns `contract-seed.sql` + `featured_read_edge_seed.sql`. Recorded in the ExecPlan Decision Log. Exit condition: two physically separate throwaway schemas (`ai_agent_station_contract` / `ai_agent_station_edge`) rather than an ordering discipline | 2/3 |
| R-34 | **Two acceptance gates are blocked on a secret the repository must never hold** | `reactor_py_ro`'s password is deliberately absent from the repo (migration takes it as session var `@reactor_py_ro_password`; never on the `mysql` command line). Repository-integration re-runs and contract re-recording therefore cannot be completed by automated work alone. Related: hunting for it in MySQL client credential stores (`~/.my.cnf`, `~/.mylogin.cnf`, `~/.config/mysql`) is **prohibited** — those files may hold unrelated or production credentials, and the task forbids reading production credentials | get as much of the task done without the secret as possible, then stop and ask the operator for it. Never `ALTER USER` a throwaway account to mint a known password as a workaround. Report the blocked gates explicitly as blocked. Exit condition: an operator-supplied secret via an out-of-band channel (env var / session variable), or a CI secret store | 3+ |
| R-35 | **Response-header identity depends on a launch flag that nothing enforces** | Java sends no `Server` header; uvicorn prepends `Server: uvicorn` unless told otherwise, and only at the protocol layer — `uvicorn/config.py` adds it *after* the app returns, so no middleware can strip it. Dropping `--no-server-header` from `backend-python/Dockerfile` (or launching `uvicorn reactor_backend.main:app` by hand, which is what the phase-1/3A docs show) silently reintroduces a public-HTTP delta on every cut-over route. Hit 2026-09-23 while closing the header delta | the flag is in the `CMD` with a "load-bearing" comment, and `tests/cutover/probes.py` records `server` as a hard-compared header (absent == `[]`), so the drill fails rather than drifts. Same class of trap as R-26's `trust_env=False`. Exit condition: the app sets the header shape itself (an explicit `__main__` entry point that passes `server_header=False`), so no invocation can forget it | 3/9 |
| R-36 | **A rollback that revives on container restart is not a rollback** | baking the cutover fragment into the frontend image means `restart: unless-stopped` brings the switch back after a rollback that only changed the container's live config | mitigated in 3B: `docker-compose.yml` bind-mounts `docker/nginx-featured-python.d/` over the baked copy, so the host directory is the single switch and its state is visible in `git status`. Residual: anyone who removes the bind-mount, or runs the image outside compose, silently loses the mitigation. Exit condition: the fragment is *not* copied into the image at all, only mounted | 3/9 |

| R-37 | **A second probe set silently reuses the first set's normalization** | `tests/cutover/probes.py::compare_to_reference` called `ProbeResult.normalized_body()`, which resolves **phase-3B's** `EXTRA_POINTERS`. `admin_probes.py` defines its own `normalized_body`, but only used it in `_attach_normalized` — so the cross-phase comparison reported raw `publishedAt`/`updatedAt` as regressions (`diffs_vs_reference=2`) while the stored hard-diff count was 0. Two different normalization configs behind one dataclass method | `compare_to_reference` takes `normalizer=` and `admin_probes.py` passes its own; the docstring says why. Any third probe set must do the same. Exit condition: the per-probe pointer lists live on `ProbeResult` itself so a set cannot supply them to one code path and not the other | 4 |
| R-38 | **Java VOs make "absent key" and "JSON null" different values** | Lombok `@Builder.Default` fields are installed by the **no-arg constructor** that Jackson binds through, and the setter then overwrites a present null with the Java primitive zero. For `FeaturedConversationAdminQueryReqVO` that means absent → `pageNo` 1 / `pageSize` 10, present-null → `0`/`0` → `Math.max(1, …)` → offset 0 / limit 1. A Python handler that reads `payload.get(key, default)` collapses the two and pages differently (`{}` 10 rows vs 1 row). `javap` on the compiled VO is the ground truth; the 2026-09-23 reading of this VO in the ExecPlan was **wrong** and was corrected 2026-09-25 | key-presence, not `.get()`, decides; the split is unit- and route-tested (`test_absent_page_fields_keep_the_lombok_defaults`, `test_json_null_page_fields_become_zero_not_the_defaults`) and pinned by 8 probe bodies. Applies to **every** remaining admin CRUD VO — check each for `@Builder.Default` on primitives before porting | 4 |
| R-39 | **A gate that forbids all 5xx rejects probes that are 5xx by design** | the admin write domain's rejection contract *is* HTTP 500 + Spring's four-key body (uncaught `IllegalArgumentException`). `probes.py`'s blanket "no 5xx ever" rule therefore failed three deliberate probes, while a blanket tolerance would let a real blow-up through | `admin_probes.py::EXPECT_STATUS` names the three ids that must be 500 *and* asserts their exact status; any other 5xx, and any listed probe that stops returning 500, still fails. The exception lives beside the probes, not in shared code — `probes.py` keeps the blanket rule for its read-only set. Exit condition: a `expect_status` key on the probe-set schema so this is declarative per set | 4 |

## Test classification (blocking gate / known baseline debt / live-paid / uncovered)

This four-way split is required by the phase 0/1 constraints and was missing until 2026-09-22. Nothing here may be moved to a weaker bucket to make a run look green.

### Blocking gate (must pass; a failure stops the migration)

- Python: `uv run ruff check .`, `uv run mypy src`, `uv run pytest -m "not integration"` in `backend-python/`.
- Java stable baseline: `mvn -B -pl Reactor-agent-case -am -DskipTests=false test` (api/domain/infrastructure/types/trigger/case).
- `docker compose config --quiet` with test-only passwords.
- Contract diffs once a slice produces them: zero unexplained differences.

### Known baseline debt (visible, currently non-blocking, never a parity excuse)

- `Reactor-agent-app`: 469 tests executed, 12 failed, 53 errored (2026-09-18 run). Dominant classes: missing/ignored `application-test.yml` and datasource fixtures, working-directory assumptions, blank Qdrant config, stale replay/deep-search/Skill/workspace/session-memory/projector assertions.
- `Reactor-agent-domain`: 178 tests passed — not debt.
- **Non-blocking exemption boundary.** `PLANS.md` forbids "把红色测试标成非阻塞". The `continue-on-error: true` on `legacy-application-baseline` is allowed **only** as baseline-debt visibility for the already-recorded 65 failures. It is not equivalence evidence, and it must never be used to absorb a migration-introduced regression. Any new failure class is a regression and blocks. See R-17 for the residual blind spot.
- No per-test inventory of the 65 exists yet — only failure categories. "Retire categories deliberately" therefore has no tracker.

### Live / paid (explicit manual suite only — never unattended CI)

Registered from the `Reactor-agent-app/pom.xml` surefire excludes (commit `4bda2a39c`, 2026-04). Those excludes **predate the migration docs and are not a migration-era ignore expansion**. 15 classes: `ElkBlacklistDataTest`, `domain/AgentTest`, `domain/AutoAgentTest`, `domain/FlowAgentExecuteTest`, `prompt/DynamicAutoAgentTest`, `prompt/TraePromptTest`, and `spring/ai/AiAgentMCPESTest`, `spring/ai/AiAgentStepTest`, `spring/ai/AiAgentTest`, `spring/ai/AiSearchMCPTest`, `spring/ai/AutoAgentTest`, `spring/ai/DynamicRateLimitQueryTest`, `spring/ai/FlowAgentMCPTest`, `spring/ai/FlowAgentTest`, `spring/ai/OpenAiTest`. POM comment: "默认回归仅执行可离线复现的测试，以下用例依赖外部模型、MCP 或独立服务环境".

This closes former blocker 2 — those tests are now classified. They stay excluded from default runs and require an explicitly invoked, auditable manual suite. Also live/manual: `POST /api/v1/admin/ai-client-model/test/{modelId}` and `/test-by-id/{id}` (external model calls), and the browser-side direct call to `${baseUrl}/v1/chat/completions` in `ui/src/services/imageGeneration.ts`.

### Uncovered / deferred

- 101 of 108 method routes have no endpoint-specific verification case; they are listed in the `api-contracts.md` deferred registry with an owner phase. Silence is not coverage.
- Multipart, binary ZIP/PDF/DOCX and the SSE corpus have no comparison capability yet.
- ~~Contract fixtures and goldens are absent.~~ **Closed 2026-09-23** — `tests/contract/fixtures/` (deterministic `contract-seed.sql`, `fake-agent-request.json`, a pinned `skills/` tree) and `tests/contract/golden/java-initial.json` (7/7 cases, byte-repeatable) now exist. What is still absent is a real Java/Python comparison, which is gated on phase 3 routes.
- The comparator still cannot measure multipart bodies, binary exports, streaming-ZIP containment, the full SSE corpus, or cross-block `last-event-ID`. Five entries in the *comparator capability deferred registry* in `api-contracts.md`. Uncovered is not reported as covered.
- MySQL integration tests and a real Compose smoke are not exercised by CI (no MySQL service container, no integration job, no docker build/contract job).
- Runtime capabilities and external systems have phase/verification/rollback entries in `inventory.md`, but every verification marked *deferred* has no case.

## Current blockers

1. The Java application test suite has 65 known failing/erroring tests; it cannot yet be used as an all-green migration gate. *(unchanged)*
2. ~~External-service tests need classification before they can be enabled in unattended CI.~~ **Closed 2026-09-22** — see the live/paid section above.
3. Production credentials and provider availability are intentionally not validated during automated migration work. *(unchanged)*
4. ~~Initial Java HTTP goldens require the full Java/tool stack and deterministic fixture identities to be running.~~ **Closed 2026-09-23** — demonstrated. A throwaway `mysqld` (port 3307, own datadir under `build/contract-recording/`, never `/usr/local/var/mysql`) plus Java under `--spring.profiles.active=prod` with test-only `spring.datasource.mysql.*` overrides is enough; the "full tool stack" is **not** required for the 7 initial GET cases. `ai_client_tool_mcp` is empty and the skill tree is pinned to `tests/contract/fixtures/skills/`, so no MCP/ES/Qdrant/provider is contacted. `tests/contract/golden/java-initial.json` is recorded, 7/7, `skipped: []`, secret scan clean, and byte-identical across a fixture re-seed. Residual fragility moved to R-27/R-28/R-29/R-31.
5. R-01/R-14 fixes live only in an uncommitted working tree — the migration must not depend on a dirty tree.
6. R-17: new `Reactor-agent-app` regressions cannot block CI.
7. R-25: the blocking Java gate does not compile `Reactor-agent-trigger` or `Reactor-agent-infrastructure`.
