services:
  mysql:
    image: mysql:8.4
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE:-ai-agent-station}
      MYSQL_USER: ${MYSQL_USER:-reactor}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:?MYSQL_PASSWORD is required}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:?MYSQL_ROOT_PASSWORD is required}
    volumes:
      - mysql-data:/var/lib/mysql
      - ./Reactor-agent-app/src/main/resources/db/schema.sql:/docker-entrypoint-initdb.d/001-schema.sql:ro
      - ./Reactor-agent-app/src/main/resources/db/data.sql:/docker-entrypoint-initdb.d/002-data.sql:ro
    healthcheck:
      test:
        - CMD-SHELL
        - mysqladmin ping -h 127.0.0.1 -u root -p"$${MYSQL_ROOT_PASSWORD}" --silent
      interval: 10s
      timeout: 5s
      retries: 20

  reactor-sandbox:
    build:
      context: .
      dockerfile: Dockerfile
      target: reactor-tool
    restart: unless-stopped
    env_file:
      - ./reactor-tool/.env
    environment:
      ENV: prod
      REACTOR_TOOL_ROLE: sandbox
      REACTOR_TOOL_HOST: 0.0.0.0
      REACTOR_TOOL_PORT: 1602
      REACTOR_TOOL_WORKERS: 1
      FILE_SAVE_PATH: /data/skilloutput
      FILE_SERVER_URL: ${PUBLIC_TOOL_URL:-http://localhost:3000/tool}/v1/file_tool
      FILE_SERVER_INTERNAL_URL: http://reactor-tool:1601/v1/file_tool
      SQLITE_DB_PATH: /data/autobots.db
      SQLITE_PATH: /data/mrag_sqlite.db
      LOG_PATH: /data/logs/reactor-sandbox.log
    volumes:
      - reactor-data:/data
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - import urllib.request; urllib.request.urlopen('http://127.0.0.1:1602/docs', timeout=3)
      interval: 10s
      timeout: 5s
      retries: 12

  reactor-tool:
    build:
      context: .
      dockerfile: Dockerfile
      target: reactor-tool
    restart: unless-stopped
    env_file:
      - ./reactor-tool/.env
    environment:
      ENV: prod
      REACTOR_TOOL_ROLE: api
      REACTOR_TOOL_HOST: 0.0.0.0
      REACTOR_TOOL_PORT: 1601
      REACTOR_TOOL_WORKERS: ${TOOL_WORKERS:-3}
      REACTOR_SANDBOX_URL: http://reactor-sandbox:1602
      FILE_SAVE_PATH: /data/skilloutput
      FILE_SERVER_URL: ${PUBLIC_TOOL_URL:-http://localhost:3000/tool}/v1/file_tool
      FILE_SERVER_INTERNAL_URL: http://reactor-tool:1601/v1/file_tool
      SQLITE_DB_PATH: /data/autobots.db
      SQLITE_PATH: /data/mrag_sqlite.db
      LOG_PATH: /data/logs/reactor-tool.log
    volumes:
      - reactor-data:/data
    depends_on:
      reactor-sandbox:
        condition: service_healthy
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - import urllib.request; urllib.request.urlopen('http://127.0.0.1:1601/docs', timeout=3)
      interval: 10s
      timeout: 5s
      retries: 12

  reactor-backend:
    build:
      context: .
      dockerfile: Dockerfile
      target: backend
    restart: unless-stopped
    env_file:
      - ./reactor-tool/.env
    environment:
      SPRING_PROFILES_ACTIVE: prod
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_DATABASE: ${MYSQL_DATABASE:-ai-agent-station}
      MYSQL_USER: ${MYSQL_USER:-reactor}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:?MYSQL_PASSWORD is required}
      REACTOR_TOOL_BASE_URL: http://reactor-tool:1601
      WORKSPACE_ROOT: /data/skilloutput
      SKILL_DIR: /app/runtime/skills
    volumes:
      - reactor-data:/data
      - reactor-data:/app/data
    depends_on:
      mysql:
        condition: service_healthy
      reactor-tool:
        condition: service_healthy

  reactor-backend-python:
    build:
      context: .
      dockerfile: backend-python/Dockerfile
    restart: unless-stopped
    environment:
      REACTOR_PY_ENVIRONMENT: prod
      REACTOR_PY_HOST: 0.0.0.0
      REACTOR_PY_PORT: 8200
      REACTOR_PY_MYSQL_HOST: mysql
      REACTOR_PY_MYSQL_PORT: 3306
      REACTOR_PY_MYSQL_DATABASE: ${MYSQL_DATABASE:-ai-agent-station}
      REACTOR_PY_MYSQL_USER: ${MYSQL_USER:-reactor}
      REACTOR_PY_MYSQL_PASSWORD: ${MYSQL_PASSWORD:?MYSQL_PASSWORD is required}
    ports:
      - "${PYTHON_BACKEND_PORT:-8200}:8200"
    volumes:
      - reactor-data:/data
    depends_on:
      mysql:
        condition: service_healthy
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - import urllib.request; urllib.request.urlopen('http://127.0.0.1:8200/internal/health/ready', timeout=3)
      interval: 10s
      timeout: 5s
      retries: 12

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: frontend
      args:
        SERVICE_BASE_URL: ${SERVICE_BASE_URL:-}
        REACTOR_TOOL_BASE_URL: ${REACTOR_TOOL_BASE_URL:-/tool}
        VITE_Mrag_TOOL_URL: ${VITE_Mrag_TOOL_URL:-/tool}
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-3000}:80"
    depends_on:
      - reactor-backend
      - reactor-tool

volumes:
  mysql-data:
  reactor-data:
